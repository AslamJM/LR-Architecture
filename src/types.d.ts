type Category = {
  _id: string;
  name: string;
  slug: string;
  image: string;
};

type ProjectCard = {
  _id: string;
  name: string;
  slug: string;
  image: string;
};

type ProjectType = {
  _id: string;
  name: string;
  description: string;
  images: string[];
  alts: string[];
};
